let config = {
    // local
    // baseAddress: 'http://localhost:3005',
    // production 
    // server address
    baseAddress: '',
    success: 200,
    forbidden: 403,
    ServerError: 500,
}

export default config;
